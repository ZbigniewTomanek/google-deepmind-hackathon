# Stage 3: Multi-Domain Ingestion

**Goal**: Ingest documents spanning all 4 semantic domains and verify domain routing assigns content to correct schemas with appropriate seed type usage.
**Dependencies**: Stage 2 DONE

---

## Steps

1. **Ingest Document 2** (Health Protocol → user_profile)
   ```bash
   curl -s -X POST http://localhost:8001/ingest/text \
     -H "Content-Type: application/json" \
     -H "Authorization: Bearer claude-code-work" \
     -d '{
       "text": "Morning Supplement Stack — Spring 2026 Update\n\nAfter blood work results from Dr. Kowalski (March 15), adjusted the protocol:\n\n1. Vitamin D3 — increased from 2000 IU to 4000 IU daily. Serum level was 28 ng/mL (target: 40-60). Taking with breakfast (fat-soluble, needs dietary fat for absorption).\n\n2. Magnesium Glycinate — 400mg before bed. Helps with sleep quality and muscle recovery. Switched from citrate form because it was causing loose stools.\n\n3. Omega-3 (EPA/DHA) — 2g daily, split morning/evening. Triglycerides were borderline at 148 mg/dL. Nordic Naturals brand, kept in fridge.\n\n4. Creatine Monohydrate — 5g daily in morning smoothie. For cognitive performance and gym recovery. No loading phase needed.\n\n5. Zinc Picolinate — 30mg with lunch. Was slightly deficient. Don'\''t take with coffee or calcium (absorption interference).\n\nDropped: Ashwagandha — was causing afternoon drowsiness. Melatonin — sleep is better with magnesium alone.\n\nBlood work recheck scheduled for June 20 to verify D3 and triglyceride improvements.\nSide effects to monitor: magnesium can cause drowsiness if dose too high. Zinc on empty stomach causes nausea.",
       "metadata": {"source": "plan29-test", "doc": "health-protocol"}
     }' | python3 -m json.tool
   ```

2. **Ingest Document 3** (Car Knowledge → domain_knowledge)
   ```bash
   curl -s -X POST http://localhost:8001/ingest/text \
     -H "Content-Type: application/json" \
     -H "Authorization: Bearer claude-code-work" \
     -d '{
       "text": "BMW F31 320d Touring — Maintenance Notes\n\nVehicle: 2016 BMW 320d Touring (F31), 2.0L diesel, 190hp, 8-speed ZF auto.\nCurrent mileage: 142,000 km. VIN: WBAXXXXXXXX.\n\nTiming chain: B47 engine uses a chain, not belt — no scheduled replacement, but listen for rattle on cold start (chain stretch symptom, common above 150k km). Replacement cost if needed: ~2500 EUR at independent shop.\n\nOil: BMW LL-04 spec, 5W-30. Castrol Edge Professional recommended. Capacity: 5.2L with filter change. Change interval: 15,000 km or 12 months (I do 10,000 km to be safe). Last change at 139,000 km (January 2026).\n\nBrake pads: Front pads replaced at 128,000 km. Rear pads still at ~4mm — should last another 15-20k km. Using Brembo P06 076 pads. Sensor wire needs replacement with pads (one-time use).\n\nDPF (Diesel Particulate Filter): Regeneration cycle every ~400-600 km. If mostly city driving, do a 30-min highway run monthly to force active regen. Warning light means manual regen needed at dealer (costs ~150 EUR). DPF replacement: ~2000 EUR.\n\nKnown issues at this mileage:\n- Swirl flap actuator can fail (P2015 code) — replacement ~400 EUR\n- Rear air springs (if equipped) leak around 140-160k km — ~300 EUR per side\n- EGR valve carbon buildup — clean every 60k km, or replace ~600 EUR",
       "metadata": {"source": "plan29-test", "doc": "car-knowledge"}
     }' | python3 -m json.tool
   ```

3. **Ingest Document 4** (Technical Architecture → technical_knowledge)
   ```bash
   curl -s -X POST http://localhost:8001/ingest/text \
     -H "Content-Type: application/json" \
     -H "Authorization: Bearer claude-code-work" \
     -d '{
       "text": "Event Sourcing with PostgreSQL — Architecture Decision Record\n\nContext: Evaluating event sourcing as the persistence pattern for the ingestion pipeline. Currently using simple INSERT-based storage with no event log.\n\nDecision: Adopt event sourcing for episode lifecycle only. Not for the full knowledge graph (too complex, poor query performance for graph traversals).\n\nArchitecture:\n- Event store: PostgreSQL table with JSONB payload column. Partitioned by month.\n- Event types: EpisodeCreated, EpisodeEmbedded, ExtractionStarted, ExtractionCompleted, ExtractionFailed, EpisodeConsolidated.\n- Projection: Materialized view for current episode state (latest event per episode_id).\n- Snapshots: Not needed initially — event count per episode is small (5-8 events max).\n\nTechnology choices:\n- PostgreSQL over Kafka: We'\''re single-node, <1000 events/day. Kafka adds operational complexity we don'\''t need. PG advisory locks handle concurrency.\n- JSONB over normalized columns: Event schemas will evolve. JSONB with a version field avoids migrations per event type.\n- Procrastinate (job queue) stays: Events trigger jobs, not replace them. The job system handles retries, dead letters, and observability.\n\nRisks:\n- Event replay performance degrades with partition count. Mitigate: yearly archive of old partitions.\n- Schema evolution of event payloads. Mitigate: version field + upcasting functions.\n\nRelated: Martin Fowler'\''s event sourcing guide, Greg Young'\''s CQRS/ES talks, Marten library (similar approach in .NET with PG).",
       "metadata": {"source": "plan29-test", "doc": "tech-architecture"}
     }' | python3 -m json.tool
   ```

4. **Ingest Document 5** (Work Context → work_context)
   ```bash
   curl -s -X POST http://localhost:8001/ingest/text \
     -H "Content-Type: application/json" \
     -H "Authorization: Bearer claude-code-work" \
     -d '{
       "text": "Sprint Review Notes — March 28, 2026\n\nTeam: Data Platform (6 engineers + PM Sarah + TL Marcus)\nSprint: 2026-Q1-S6 (March 18 - March 28)\n\nCompleted:\n1. PLAT-1234: Migration runner v2 — automatic rollback on failure. Took 5 story points. Marcus did the implementation, I reviewed. Good test coverage.\n2. PLAT-1245: Dashboard latency widget — P95 latency chart for ingestion pipeline. Connected to Grafana. 3 story points. Julia'\''s first solo feature — clean work.\n3. PLAT-1251: Fix connection pool exhaustion under load — was hitting 100 connections during batch ingestion. Increased pool to 50, added connection timeout of 30s. Root cause: long-running transactions in extraction pipeline holding connections. 2 story points.\n\nCarried over:\n- PLAT-1260: Schema versioning for graph migrations — blocked on decisions about backward compatibility. Need architecture review with CTO next week.\n- PLAT-1248: Embedding service failover — waiting for GCP quota increase.\n\nDemo: Showed the migration runner rollback to stakeholders. Good reception. Product manager Tomek asked about self-service schema creation — added to backlog as PLAT-1270.\n\nRetro highlights:\n- Positive: PR review turnaround improved (avg 4h → 2h)\n- Action item: Set up automated performance regression tests (assigned to me, due April 11)\n\nNext sprint planning: Monday March 31, 10:00 with Sarah.",
       "metadata": {"source": "plan29-test", "doc": "work-context"}
     }' | python3 -m json.tool
   ```

5. **Wait for all extraction jobs to complete** — use the polling pattern from `resources/commands.md`

6. **Verify domain routing** — check which schemas were created and whether content went to expected domains
   ```bash
   # List all schemas
   curl -s http://localhost:8001/admin/graphs \
     -H "Authorization: Bearer admin-token" | python3 -m json.tool

   # Check domain routing logs
   grep "domain_routing_completed" log/agent_actions.log
   grep "domain_classification_result" log/agent_actions.log
   ```
   Expected schemas:
   - `ncx_ccwork__personal` (personal graph, always created)
   - `ncx_shared__user_profile` (health protocol, weekly note personal content)
   - `ncx_shared__technical_knowledge` (architecture ADR)
   - `ncx_shared__work_context` (sprint review)
   - `ncx_shared__domain_knowledge` (car knowledge)

7. **Run diagnostics per schema** — use the all-in-one query from `resources/queries.md`
   for each schema created. Record results.

8. **Check seed type usage** — for each shared schema, verify that domain-specific
   seed types are being used (not ignored in favor of novel types)
   ```bash
   # Example for user_profile schema
   docker compose exec postgres psql -U neocortex -d neocortex -c \
     "SELECT nt.name, COUNT(n.id) AS usage
      FROM ncx_shared__user_profile.node_type nt
      LEFT JOIN ncx_shared__user_profile.node n ON n.type_id = nt.id
      GROUP BY nt.id, nt.name
      HAVING COUNT(n.id) > 0
      ORDER BY usage DESC;"
   ```

---

## Verification

- [ ] All 4 documents ingested successfully (`status: "stored"`)
- [ ] All extraction jobs completed (no failures)
- [ ] Domain routing created expected shared schemas (at least 3 of 4 domains)
- [ ] Each shared schema has nodes (not empty)
- [ ] Seed types are being used (at least some seed node types have usage > 0 per schema)
- [ ] No garbage types across any schema
- [ ] No instance-level types across any schema
- [ ] Node type count per schema is reasonable (<20 per schema at this volume)
- [ ] Ontology agent used tools before proposing in all extraction runs

---

## Commit

No commit — testing stage only. Record observations in the index's Notes column.
